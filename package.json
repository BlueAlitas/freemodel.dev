{
  "name": "freemodel-status",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Status page + poller for the freemodel.dev LLM gateway.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
